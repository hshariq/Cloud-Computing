#!/usr/bin/env bash
# One command to start everything (cluster + VNF + Hello World).
# From repo root: ./run
set -e
REPO_ROOT="$(cd "$(dirname "$0")" && pwd)"
cd "$REPO_ROOT"
chmod +x scripts/ensure-cluster.sh scripts/run-cloud.sh scripts/deploy/*.sh 2>/dev/null || true
exec "$REPO_ROOT/scripts/run-cloud.sh"


# unset KUBECONFIG
# kubectl port-forward -n vnf-demo svc/vnf-nginx-lb 9080:80
# python3 scripts/monitoring/run_experiment.py --url http://127.0.0.1:9080 --duration 30 --out-dir results/cloud


# export KUBECONFIG=${HOME}/.kube/edge-k3s.yaml
# kubectl port-forward -n vnf-demo svc/vnf-nginx-lb 9080:80
# python3 scripts/monitoring/run_experiment.py --url http://127.0.0.1:9080 --duration 30 --out-dir results/edge

# ./scripts/stop-all.sh